[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite needs a modern Node; the site's default is pinned to whatever
  # was current when it was created on Netlify years ago
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
